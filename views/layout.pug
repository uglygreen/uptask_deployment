doctype html
html(lang="en")
    head
        include partials/header    
    body 
        header.barra
            h1 UpTask - Administrador de Poryectos
            a(href="/cerrar-sesion") Cerrar Sesión
            
        main.contenedor
            aside.contenedor-proyectos 
                .panel.crear-proyecto 
                    a(href="/nuevo-proyecto" class="boton") Nuevo Proyeto
                        span + 
                .panel.lista-proyectos 
                    h2 Proyectos
                    ul#proyectos.proyecctos 
                        if(proyectos.length)
                            each proyecto in proyectos 
                                li
                                    a(href=`/proyectos/${proyecto.url}`)= proyecto.nombre 

            block contenido

        script(src="/dist/bundle.js")